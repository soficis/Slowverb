{
  "name": "slowverb-web-ts",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/core-worker/tsconfig.json && tsc --noEmit -p packages/core/tsconfig.json",
    "lint:ts": "eslint \"packages/*/src/**/*.ts\" \"tests/**/*.ts\" --max-warnings=0",
    "check:ts": "npm run typecheck && npm run lint:ts && npm run build:ts",
    "test:dart": "flutter test --platform chrome --reporter compact test/data/repositories/preset_repository_web_test.dart && flutter test --platform chrome --reporter compact test/data/repositories/project_repository_web_test.dart && flutter test --platform chrome --reporter compact test/domain/entities/audio_metadata_test.dart && flutter test --platform chrome --reporter compact test/domain/entities/parameter_definitions_test.dart && flutter test --platform chrome --reporter compact test/domain/exceptions/app_exceptions_test.dart && flutter test --platform chrome --reporter compact test/domain/exceptions/slowverb_exceptions_test.dart && flutter test --platform chrome --reporter compact test/providers/audio_editor_provider_test.dart && flutter test --platform chrome --reporter compact test/providers/mastering_provider_test.dart && flutter test --platform chrome --reporter compact test/widget_test.dart",
    "check:dart": "flutter analyze --no-pub && npm run test:dart",
    "check": "npm run check:ts && npm run check:dart",
    "build:ts": "npm run build -w @slowverb/shared && npm run build -w @slowverb/core-worker && npm run build -w @slowverb/core",
    "sync:bridge": "node scripts/sync_bridge_assets.mjs",
    "vercel-build": "bash scripts/vercel_build.sh",
    "build": "npm run build:ts && npm run sync:bridge",
    "test:unit": "node --test packages/shared/test/filter-compiler.test.mjs && vitest run packages/core-worker/test",
    "test": "playwright test"
  },
  "devDependencies": {
    "@ffmpeg/core": "^0.12.6",
    "@playwright/test": "^1.49.0",
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "eslint": "^8.57.1",
    "tsup": "^8.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
